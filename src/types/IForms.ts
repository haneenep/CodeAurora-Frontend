export interface UserSignupFormType {
    userName: string;
    email: string;
    password: string;
    confirmPassword: string;
  }

export interface UserSiginFormType {
  email: string;
  password: string;
}